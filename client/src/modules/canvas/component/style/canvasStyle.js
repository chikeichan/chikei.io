Trio.Module.export('canvasStyle', function() {
    var style = {
        ':host': {
            'display': 'flex',
            'flex': '1 0 auto',
            'cursor': 'default'
        }
    };

     return style;
});